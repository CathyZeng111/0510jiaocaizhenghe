# 功能 2：单本教材知识图谱构建

当前实现提供本地启发式知识图谱构建能力：按章节抽取高频术语作为知识点节点，并生成 `contains`、`prerequisite`、`parallel`、`applies_to` 四类关系中的多类关系。

接口：

- `POST /api/graphs/build`
- `GET /api/graphs/{textbook_id}`

后续可将 `backend/app/knowledge_graph.py` 中的术语抽取替换为 LLM JSON 输出，保持图谱 schema 不变。
