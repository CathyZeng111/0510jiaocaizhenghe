# 功能 4：跨教材知识图谱整合

当前实现使用轻量语义对齐完成多教材图谱合并：

- 规范化名称匹配
- token Jaccard 相似度
- 定义文本 Jaccard 相似度
- 输出 `merge`、`keep`、`remove` 决策
- 生成整合后图谱和节点压缩比统计

接口：

- `POST /api/integration/build`
- `POST /api/integration/feedback`

教师反馈支持保留、删除、拆分、合并等自然语言指令的简单解析。
