# AI 全栈黑客松提交要求

> 来源：`第一届AI全栈黑客松赛题.pdf` 第 4 节“提交要求”。本文档整理为 Markdown，便于转发给其他 agent 执行交付检查与打包。

## 4.1 提交物

每位参赛者必须提交以下两项，缺一不可：

| 提交项 | 要求 | 说明 |
| --- | --- | --- |
| GitHub 仓库链接 | 公开可访问（Public） | 包含全部源代码和文档 |
| 在线部署链接 | 能在浏览器中打开并正常使用 | 部署到魔搭创空间或其他公网可访问平台 |

未同时提交以上两项的作品视为未完成，不进入评审流程。

## 4.2 仓库结构规范

推荐仓库结构：

```text
你的项目/
├── .gitignore                 # 必须排除教材 PDF 文件
├── README.md                  # 项目说明：环境依赖、安装步骤、配置说明、运行命令
├── docs/
│   ├── 需求分析.md             # 问题分解与子问题分析
│   ├── 系统设计.md             # 架构设计、数据流、技术选型
│   ├── Agent 架构说明.md       # Agent 架构设计决策与论证（核心评分文档）
│   └── 接口文档.md             # API 接口定义（可选）
├── src/                       # 源代码，前后端分离或合并均可
├── report/
│   └── 整合报告.md             # 以 7 本教材为例的整合报告
├── requirements.txt           # Python 依赖（如使用 Python）
├── package.json               # 前端依赖（如使用 Node.js）
└── docker-compose.yml         # 容器化部署配置（加分项）
```

重要：不要将教材 PDF 文件推送到 GitHub 仓库。7 本教材总计约 826MB，GitHub 单文件限制 100MB，推送会失败。请在 `.gitignore` 中添加：

```gitignore
# 教材数据（不上传到 GitHub）
data/textbooks/*.pdf
*.pdf
```

教材文件由赛方单独提供，评审时会使用赛方的教材数据测试系统。代码应支持用户在前端上传教材文件，而不是依赖仓库内的固定文件。

## 4.3 提交方式

通过赛事提交表单填写：

- 姓名
- 学号
- GitHub 仓库链接
- 部署链接
- P2 挑战加分项技术报告飞书文档链接（选填）

## 4.4 提交时间

- 必交项（GitHub 仓库链接 + 部署链接）：鼓励尽早提交。提前提交不影响继续更新代码，评审以截止时间前的最后一次 git commit 为准。早交晚交不影响评分，但早交可以确认提交流程没有问题。
- 选交项（P2 技术报告飞书文档）：可以在比赛结束后 24 小时内补交。技术报告需要完整实验数据和深入思考，可额外打磨。
- 所有代码评审以截止时间前的最后一次 git commit 为准，截止后的 commit 不计入评审。

## 给其他 Agent 的打包检查清单

打包或推送前必须确认：

- `README.md` 存在，并包含环境依赖、安装步骤、配置说明、运行命令、使用说明。
- `docs/需求分析.md`、`docs/系统设计.md`、`docs/Agent 架构说明.md` 存在。
- `report/整合报告.md` 存在。
- `requirements.txt` 和/或 `package.json` 存在，且与项目实际技术栈一致。
- `.env.example` 可提交；`.env` 不可提交。
- 不提交 `*.pdf`、`textbooks/`、`textbooks.zip`、`data/uploads/`、`data/processed/`、`node_modules/`、`frontend/dist/`、本地 conda/venv、`.DS_Store`。
- 压缩包内应保留源代码、文档、依赖声明、Dockerfile/部署配置；不应保留教材原文或本地缓存。
- 最终比赛仍需提交公开 GitHub 仓库链接和公网部署链接，单独的 zip 压缩包不能替代这两项。
