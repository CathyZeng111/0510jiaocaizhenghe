# 知识图谱交互功能开发笔记

## 开发建议

赛题推荐 D3.js、ECharts、Cytoscape.js、AntV G6 任选一个即可。当前项目已经安装并使用 `cytoscape`，所以最稳的方案是继续用 Cytoscape.js 完成验收项，不建议临近开发时切换到 G6。

G6 的学习价值可以保留为后续增强方向：如果之后需要更强的 Combo 分组、内置插件、AntV 风格图谱应用、复杂布局和图谱探索工具，再迁移到 G6。当前黑客松验收目标更适合优先把现有 Cytoscape 图谱做完整、稳定、可演示。

## 赛题验收项拆解

| 要求 | 当前项目状态 | 待完善点 |
| --- | --- | --- |
| 浏览器渲染知识图谱 | 已有 `GraphView` | 保持构建后自动渲染 |
| 点击节点展示详情 | 已有 `selectedNode` 详情区 | 增加原文出处 `source_excerpt`，展示更完整 |
| 节点频次可视化 | 已用 `frequency` 影响节点大小 | 增加颜色深度或图例说明更直观 |
| 教材来源区分 | 已用 `textbook_title` 做颜色哈希 | 增加来源图例，避免评委看不懂颜色含义 |
| 鼠标滚轮缩放 | Cytoscape 默认支持 | 保留 `wheelSensitivity` |
| 拖拽移动画布 | Cytoscape 默认支持 | 确认 `userPanningEnabled` 不被禁用 |
| 拖拽移动节点 | Cytoscape 默认支持 | 确认节点 `grabbable` |
| 搜索高亮 | 已有关键词高亮 | 搜索后可自动居中第一个匹配节点 |

## 最小可交付版本

待会开发这个功能时，建议先做这 5 个小改动：

1. 节点详情区补充 `source_excerpt`，标题写清楚“原文出处”。
2. 图谱上方或下方加一个“教材颜色图例”，列出每本教材对应颜色。
3. 搜索命中节点后，自动 `fit` 或 `center` 到匹配节点。
4. 点击节点时，同时高亮该节点和一阶邻居，弱化无关节点。
5. 空图状态展示“请先上传/加载教材并点击构建图谱”，避免白屏像 bug。

这样可以更直接满足验收标准，而不用重构技术栈。

## 当前数据字段如何用于图谱

后端 `KnowledgeNode` 字段：

```ts
type KnowledgeNode = {
  id: string;
  name: string;
  definition: string;
  category: string;
  textbook_id: string;
  textbook_title: string;
  chapter: string;
  page: number;
  frequency: number;
  source_excerpt: string;
};
```

前端 Cytoscape 节点建议：

```js
{
  data: {
    id: node.id,
    label: node.name,
    frequency: node.frequency || 1,
    book: node.textbook_title,
    category: node.category,
    raw: node,
  },
  grabbable: true,
}
```

前端 Cytoscape 边建议：

```js
{
  data: {
    id: `edge_${index}`,
    source: edge.source,
    target: edge.target,
    label: edge.relation_type,
    description: edge.description,
  },
}
```

## 视觉编码规则

### 教材来源

用颜色区分教材来源：

```js
"background-color": (ele) => palette(ele.data("book"))
```

建议增加图例：

```jsx
function GraphLegend({ graph }) {
  const books = Array.from(new Set((graph.nodes || []).map((node) => node.textbook_title))).filter(Boolean);

  return (
    <div className="graph-legend">
      {books.map((book) => (
        <span key={book}>
          <i style={{ background: palette(book) }} />
          {book}
        </span>
      ))}
    </div>
  );
}
```

### 频次

用节点大小反映频次：

```js
width: (ele) => 28 + Math.min(36, ele.data("frequency") * 8),
height: (ele) => 28 + Math.min(36, ele.data("frequency") * 8),
```

可选增强：频次越高边框越深。

```js
"border-width": (ele) => Math.min(6, 1 + ele.data("frequency")),
"border-color": (ele) => ele.data("frequency") > 1 ? "#1f2937" : "#ffffff",
```

## 节点点击详情

详情区至少包含：

- 名称：`selectedNode.name`
- 定义：`selectedNode.definition`
- 所在章节：`selectedNode.chapter`
- 教材：`selectedNode.textbook_title`
- 页码：`selectedNode.page`
- 原文出处：`selectedNode.source_excerpt`

建议 JSX：

```jsx
<div className="node-detail">
  {selectedNode ? (
    <>
      <strong>{selectedNode.name}</strong>
      <span>{selectedNode.textbook_title} · {selectedNode.chapter} · 第 {selectedNode.page} 页</span>
      <p>{selectedNode.definition}</p>
      {selectedNode.source_excerpt && (
        <blockquote>{selectedNode.source_excerpt}</blockquote>
      )}
    </>
  ) : (
    <span>点击图谱节点查看名称、定义、章节和原文出处</span>
  )}
</div>
```

## 搜索交互

当前已有搜索高亮。建议再加“自动聚焦第一个命中节点”：

```js
useEffect(() => {
  const cy = cyRef.current;
  if (!cy) return;

  cy.nodes().removeClass("highlight");

  const keyword = search.trim();
  if (!keyword) return;

  const matched = cy.nodes().filter((node) => node.data("label").includes(keyword));
  matched.addClass("highlight");

  if (matched.length > 0) {
    cy.animate({
      fit: { eles: matched, padding: 80 },
      duration: 240,
    });
  }
}, [search]);
```

## 点击节点时高亮邻居

这会让评委直观看到“图谱交互”不是静态图片。

```js
cy.on("tap", "node", (event) => {
  const node = event.target;
  const neighborhood = node.closedNeighborhood();

  cy.elements().addClass("dimmed");
  neighborhood.removeClass("dimmed");
  node.addClass("selected");

  onSelect(node.data("raw"));
});

cy.on("tap", (event) => {
  if (event.target === cy) {
    cy.elements().removeClass("dimmed selected");
    onSelect(null);
  }
});
```

配套样式：

```js
{
  selector: ".dimmed",
  style: { opacity: 0.18 },
},
{
  selector: ".selected",
  style: {
    "border-color": "#f2a900",
    "border-width": 5,
  },
}
```

## Cytoscape 配置建议

```js
const cy = cytoscape({
  container: ref.current,
  elements,
  style,
  layout: {
    name: "cose",
    animate: false,
    nodeRepulsion: 9000,
    idealEdgeLength: 120,
  },
  wheelSensitivity: 0.18,
  userZoomingEnabled: true,
  userPanningEnabled: true,
  autoungrabify: false,
});
```

## 如果后续改用 G6

G6 的对应方案：

- `node.palette.field = textbook_title`：教材来源颜色
- `node.style.size = frequency`：频次大小
- `behaviors = ['drag-canvas', 'zoom-canvas', 'drag-element', 'click-select', 'hover-activate']`
- `plugins = ['tooltip', 'legend', 'minimap']`
- `combo`：按教材或章节分组

G6 更适合图谱功能继续扩展后的版本，但当前开发优先级应该是：先满足赛题验收，再考虑迁移。

## 开发前检查清单

- 加载本地教材后，有节点和边。
- 点击“构建图谱”后，浏览器中心区域出现图谱。
- 鼠标滚轮能缩放。
- 鼠标拖拽空白处能移动画布。
- 鼠标拖拽节点能移动单个节点。
- 点击任意节点，详情区显示名称、定义、章节、页码、原文出处。
- 多本教材节点颜色不同，并有图例说明。
- 重复出现的知识点节点更大或视觉更突出。
- 搜索关键词后，匹配节点高亮并自动进入视野。
- 空图、加载中、错误状态有明确提示。

## 推荐开发顺序

1. 先完善现有 Cytoscape `GraphView`，不要换库。
2. 补详情字段和图例，这是验收最容易看见的部分。
3. 加搜索自动聚焦和邻居高亮，让交互感更明显。
4. 最后微调视觉样式，保证颜色、大小、文字不拥挤。

一句话策略：用当前 Cytoscape 方案做出一个能演示、能解释、能过验收的图谱，再把 G6 作为后续增强储备。
